<script>
import { mapGetters } from 'vuex';
import PartialLoader from 'js/components/UI/PartialLoader';

export default {
    name: 'variant-content-contacts',
    components: {
        PartialLoader
    },
    props: {
        productLine: {
            type: String,
            required: true
        },
        translations: {
            type: Object,
            required: true
        }
    },
    data() {
        return {
            fetchingItemContacts: true
        };
    },
    computed: {
        ...mapGetters([
            'internalItemContacts',
            'dotTeamUrl',
            'isUserEmployee'
        ])
    },
    mounted() {
        this.$store.dispatch('getDotInternalItemContacts', this.productLine).then(() => {
            this.fetchingItemContacts = false;
        });
    }
};
</script>